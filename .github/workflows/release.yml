name: Release

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:

# Cancel in-flight runs when a new release dispatch arrives (e.g.
# tag re-pushed, manual re-run). Keyed by workflow + ref/tag/input
# so concurrent releases don't collide.
concurrency:
  group: ${{ github.workflow }}-${{ github.ref || github.event.inputs.tag || github.run_id }}
  cancel-in-progress: true

permissions:
  contents: write

jobs:
  validate:
    name: Validate tag matches manifests
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0

      - name: Run release readiness check
        run: |
          set -euo pipefail
          TAG="${GITHUB_REF_NAME#v}"
          echo "Validating tag v${TAG} against working tree manifests..."
          bash scripts/check-release-readiness.sh "${TAG}"

  # Build only — does NOT create a release. Each matrix job produces platform
  # bundles and uploads them as workflow artifacts. The `upload` job
  # below is the single source of release creation, eliminating the
  # tauri-action@v1.0.0 race that produced duplicate GitHub Releases.
  build:
    needs: validate
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: ubuntu-22.04
            args: ""
            rust_target: x86_64-unknown-linux-gnu
            artifact_suffix: linux-x86_64
          - platform: windows-latest
            args: ""
            rust_target: x86_64-pc-windows-msvc
            artifact_suffix: windows-x86_64
          - platform: macos-latest
            args: "--target aarch64-apple-darwin"
            rust_target: aarch64-apple-darwin
            artifact_suffix: macos-aarch64
          - platform: macos-latest
            args: "--target x86_64-apple-darwin"
            rust_target: x86_64-apple-darwin
            artifact_suffix: macos-x86_64

    runs-on: ${{ matrix.platform }}

    steps:
      - name: Checkout
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0

      - name: Install Linux dependencies
        if: matrix.platform == 'ubuntu-22.04'
        run: |
          sudo apt-get update
          sudo apt-get install -y libwebkit2gtk-4.1-dev libappindicator3-dev librsvg2-dev patchelf

      - name: Setup Node.js
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: "lts/*"
          cache: "npm"

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@29eef336d9b2848a0b548edc03f92a220660cdb8 # stable
        with:
          toolchain: stable
          targets: ${{ matrix.rust_target }}

      - name: Rust cache
        uses: swatinem/rust-cache@c19371144df3bb44fab255c43d04cbc2ab54d1c4 # v2.9.1
        with:
          workspaces: src-tauri

      - name: Install frontend dependencies
        run: npm ci

      - name: Configure Windows code signing
        if: matrix.platform == 'windows-latest'
        shell: pwsh
        env:
          WINDOWS_CERTIFICATE: ${{ secrets.WINDOWS_CERTIFICATE }}
          WINDOWS_CERTIFICATE_PASSWORD: ${{ secrets.WINDOWS_CERTIFICATE_PASSWORD }}
        run: |
          if (-not $env:WINDOWS_CERTIFICATE) {
            Write-Host "No Windows signing certificate configured, skipping..."
            exit 0
          }
          New-Item -ItemType Directory -Force -Path certificate | Out-Null
          Set-Content -Path certificate/tempCert.txt -Value $env:WINDOWS_CERTIFICATE
          certutil -decode certificate/tempCert.txt certificate/certificate.pfx | Out-Null
          Remove-Item certificate/tempCert.txt
          $securePassword = ConvertTo-SecureString -String $env:WINDOWS_CERTIFICATE_PASSWORD -Force -AsPlainText
          $cert = Import-PfxCertificate -FilePath certificate/certificate.pfx -CertStoreLocation Cert:\CurrentUser\My -Password $securePassword
          $config = Get-Content src-tauri/tauri.conf.json -Raw | ConvertFrom-Json
          $config.bundle | Add-Member -Force -MemberType NoteProperty -Name windows -Value ([pscustomobject]@{
            certificateThumbprint = $cert.Thumbprint
            digestAlgorithm = 'sha256'
            timestampUrl = 'http://timestamp.digicert.com'
          })
          $config | ConvertTo-Json -Depth 100 | Set-Content src-tauri/tauri.conf.json

      # Build-only mode: omit tagName/releaseName/releaseId. Per tauri-action@v1.0.0
      # README: "If you only want to build the app without having the action upload
      # any assets, for example if you want to only use actions/upload-artifact,
      # simply omit tagName, releaseName and releaseId."
      - name: Build bundles (no release)
        uses: tauri-apps/tauri-action@1deb371b0cd8bd54025b384f1cd735e725c4060f # v1.0.0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          TAURI_SIGNING_PRIVATE_KEY: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY }}
          TAURI_SIGNING_PRIVATE_KEY_PASSWORD: ""
        with:
          args: ${{ matrix.args }}

      # Stage the Windows portable exe under its public name so the upload job
      # can publish it as `SQLPilot.exe` (matches the historical asset name).
      - name: Stage portable exe (Windows)
        if: matrix.platform == 'windows-latest'
        shell: pwsh
        run: |
          $exe = "src-tauri/target/release/sqlpilot.exe"
          $dest = "SQLPilot.exe"
          if (-not (Test-Path $exe)) { throw "Expected binary not found: $exe" }
          Copy-Item $exe $dest -Force

      # Upload every artifact the upload job will need: the bundle tree
      # (containing .deb/.rpm/.AppImage/.msi/.exe/.dmg + .sig) and, on
      # Windows, the staged portable exe at the repo root.
      - name: Upload build artifacts
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: bundles-${{ matrix.artifact_suffix }}
          path: |
            src-tauri/target/release/bundle/
            SQLPilot.exe
          if-no-files-found: error

  # Single source of release creation. All 4 build jobs race to complete,
  # but only this job touches the GitHub Releases API — no race possible.
  upload:
    name: Create release and upload assets
    needs: [build]
    runs-on: ubuntu-22.04
    steps:
      - name: Download all build artifacts
        uses: actions/download-artifact@37930b1c2abaa49bbe596cd826c3c89aef350131 # v7.0.0
        with:
          path: artifacts
          merge-multiple: true

      - name: Create draft release and upload assets
        uses: softprops/action-gh-release@3bb12739c298aeb8a4eeaf626c5b8d85266b0e65 # v2.6.2
        with:
          tag_name: ${{ github.ref_name }}
          name: "SQLPilot ${{ github.ref_name }}"
          body: |
            ## Downloads
            | Platform | File |
            |----------|------|
            | Windows (portable) | `SQLPilot.exe` |
            | Windows (installer) | `.msi` or `-setup.exe` |
            | macOS (Apple Silicon) | `aarch64.dmg` |
            | macOS (Intel) | `x64.dmg` |
            | Linux | `.deb` or `.AppImage` |
          draft: true
          prerelease: false
          fail_on_unmatched_files: true
          files: |
            artifacts/**/*.AppImage
            artifacts/**/*.deb
            artifacts/**/*.rpm
            artifacts/**/*.msi
            artifacts/**/*.exe
            artifacts/**/*.dmg
            artifacts/**/*.sig
            artifacts/**/*.tar.gz
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  generate-update-manifest:
    name: Generate update manifest
    needs: [upload]
    runs-on: ubuntu-22.04
    steps:
      - name: Checkout
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
      - name: Setup Node.js
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: "lts/*"
          cache: "npm"
      - name: Install dependencies
        run: npm ci
      - name: Download release artifacts
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: gh release download ${{ github.ref_name }}
      - name: Sign artifacts and generate manifest
        env:
          TAURI_SIGNING_PRIVATE_KEY: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY }}
          TAURI_SIGNING_PRIVATE_KEY_PASSWORD: ""
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          TAG="${{ github.ref_name }}"
          VERSION="${TAG#v}"

          # Generate signatures for each platform binary
          for file in *.AppImage *.deb *.rpm *.msi *.exe *.dmg *.tar.gz; do
            [ -f "$file" ] || continue
            echo "Signing $file..."
            npx tauri signer sign "$file"
          done

          rm -f /tmp/sign-key

          # Build the manifest JSON
          echo "{" > update-manifest.json
          echo "  \"version\": \"$VERSION\"," >> update-manifest.json

          # min_app_version: most recent tag excluding the one being shipped.
          # Plugin honors this: current-version users whose app is below the
          # threshold don't see the update. Falls back to omitting the field
          # on the very first release.
          PREV_TAG=$(git tag --sort=-v:refname 2>/dev/null | grep -v "^${TAG}$" | head -1 | sed 's/^v//')
          if [ -n "$PREV_TAG" ]; then
            echo "  \"min_app_version\": \"$PREV_TAG\"," >> update-manifest.json
          fi

          echo "  \"notes\": \"See https://github.com/EVWorth/sqlpilot/releases/tag/$TAG\"," >> update-manifest.json
          echo "  \"pub_date\": \"$(date -u +%Y-%m-%dT%H:%M:%SZ)\"," >> update-manifest.json
          echo "  \"platforms\": {" >> update-manifest.json

          first=true
          for sigfile in *.sig; do
            [ -f "$sigfile" ] || continue
            base="${sigfile%.sig}"

            case "$base" in
              *_amd64.AppImage)     key="linux-x86_64" ;;
              *_amd64.deb)          key="linux-deb" ;;
              *aarch64.rpm)        key="linux-aarch64" ;;
              *x86_64.rpm)         key="linux-rpm" ;;
              *_x64_en-US.msi)      key="windows-x86_64" ;;
              *_x64-setup.exe)      key="windows-x86_64-nsis" ;;
              *_x64.dmg)            key="darwin-x86_64" ;;
              *_aarch64.dmg)        key="darwin-aarch64" ;;
              *)                    echo "Unknown platform for $sigfile, skipping..." ; continue ;;
            esac

            # Build download URL matching GitHub's asset naming
            url="https://github.com/EVWorth/sqlpilot/releases/download/$TAG/$base"
            sig=$(cat "$sigfile")

            $first || echo "," >> update-manifest.json
            echo "    \"$key\": {" >> update-manifest.json
            echo "      \"url\": \"$url\"," >> update-manifest.json
            echo "      \"signature\": \"$sig\"" >> update-manifest.json
            echo "    }" >> update-manifest.json
            first=false
          done

          echo "  }" >> update-manifest.json
          echo "}" >> update-manifest.json

          echo "Generated manifest:"
          cat update-manifest.json
          mv update-manifest.json latest.json

      - name: Upload update manifest
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: gh release upload ${{ github.ref_name }} latest.json --clobber
