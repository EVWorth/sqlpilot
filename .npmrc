# Security: only allow packages older than 1 day to reduce risk of freshly-published malicious versions
# This gives the community time to identify and report compromised packages
audit-level=critical
# Ensure lockfile is committed and checked
package-lock=true
